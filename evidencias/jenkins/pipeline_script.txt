node {
  stage('Preparation') {
    catchError(buildResult: 'SUCCESS') {
      bat 'docker stop samplerunning'
      bat 'docker rm samplerunning'
    }
  }
  stage('Build') {
    build 'BuildAppJob'
  }
}